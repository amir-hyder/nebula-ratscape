# Shared API contracts

The canonical provisional TypeScript types are in `backend/src/contracts/index.ts`. Frontend Dart models correspond to these concepts. Agree on identifiers and timestamps (ISO 8601 with timezone) before the three backend modules implement adapters. Examples in `examples/` are simulated; update them as the API evolves.
