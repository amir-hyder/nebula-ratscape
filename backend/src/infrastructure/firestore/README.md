# Firestore (future)

Persist destinations, schedules, selected journey state and meaningful events. Avoid storing every polling response. Keep the browser from writing directly to Firestore while this project has no authentication.
