# HTTP API (future)

Planned endpoints: `GET /journeys/current`, `POST /journeys/start`, `POST /journeys/:id/location`, `POST /journeys/:id/actions`, `GET /destinations`, `PUT /destinations/:id`, `GET /notifications`, and `POST /demo/events` in demo mode. No HTTP server is active yet.
