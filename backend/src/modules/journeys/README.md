# Journey coordination

Owner 3: journey endpoints, schedules, child actions, alert rules, one-minute monitoring window, Firestore state and demo replay. Consume route candidates and conditions; return `JourneyDecision`. Explicit child arrival confirmation creates the arrival event.
